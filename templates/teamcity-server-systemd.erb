[Unit]
Description=<%= @service %>
Requires=network.service

[Service]
Type=forking
ExecStart=<%= @bin_dir %>/sys-server.sh start
ExecStop=<%= @bin_dir %>/sys-server.sh stop
User=<%= @user %>
Restart=always
RestartSec=5s

[Install]
WantedBy=multi-user.target
